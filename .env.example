# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored and must NEVER be committed.

# Base URL of the deployed Micronaut backend.
# In development, you can point this at the AWS instance or a local instance.
# Example: http://3.122.251.179:8080
NUXT_PUBLIC_API_BASE_URL=
